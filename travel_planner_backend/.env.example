# Environment variables for Travel Planner Backend (FastAPI)

# Database URL (SQLAlchemy format). Defaults to SQLite file if not set.
# Examples:
#   sqlite:///./travel_planner.db
#   sqlite:////data/travel_planner.db
#   postgresql+psycopg2://user:password@host:5432/dbname
TRAVEL_PLANNER_DB_URL=sqlite:///./travel_planner.db

# Optional CORS origin for frontend (left open by default in main.py)
# FRONTEND_ORIGIN=http://localhost:3000
